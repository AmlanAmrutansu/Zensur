🚀 Zensure: Production-Ready Pivot Prompt"I have 24 hours to finish Zensure, an AI-powered income protection system for gig workers. We are pivoting to a Serverless Architecture to ensure 100% compatibility with Netlify. Please execute these high-priority modifications immediately:1. AI Service Integration (Replacing Python)Create app/api/ai/analyze/route.ts. This route must use Groq's Llama-3.1-8b-instant (or Nvidia NIM) to handle all logic.Implement two modes:'risk': Takes user profile data and returns a risk_score (0-1) and suggested_premium.'fraud': Takes activity data (distance/hours) and returns a fraud_score and is_fraud boolean.Ensure all AI calls are wrapped in try/catch blocks with safe fallback values if the API fails.2. The Core Decision Engine (The Logic)Create a server-side function/route for Automatic Payout Calculation.Trigger Logic: Hardcode the thresholds: Rain > 70mm, Temp > 42°C, or AQI > 400.The Formula: Calculate payout as:$$payout = \min(loss - deductible, payout\_cap)$$Ensure this logic pulls real-time weather/AQI data using the provided API keys.3. High-Fidelity UI/UX (Mobile-First)Theme: Use a sleek, "FinTech" dark mode with Tailwind CSS. Use Emerald/Green for safety and Amber for alerts.Dashboard: * Add a Live Activity Tracker component with a pulsing 'Recording' indicator.Use Recharts to show a 'Earnings vs. Predicted' line graph.Add Lucide-react icons for Weather, Risk Level, and Wallet.UX: Add a 'Simulation Mode' button that triggers a fake weather event so I can demo the automatic payout instantly.4. Database & Auth HardwareEnsure Supabase Auth is fully linked to the users table.On user registration, automatically trigger the 'Risk AI' to set their initial policy in the policies table.All activity logs must be saved to the activities table every 60 seconds during 'active' mode.5. Netlify & Deployment PrepCreate a public/_redirects file with /* /index.html 200.Update package.json to include recharts, lucide-react, and axios.Ensure all API calls use process.env for keys.DO NOT use any Python. Everything must be in Next.js (TypeScript). Start by building the AI Route and the Dashboard UI first. I will provide the API keys in the Secrets tool."